/**
 * Backwards-compatible re-export of the design-system ProgressBar.
 *
 * The component was promoted into `src/components/ui/ProgressBar.jsx`
 * as part of the design-system refactor; this re-export keeps the
 * historical import path working for any external consumers.
 */
export { default } from './ui/ProgressBar.jsx';
